# crm_saas_aicoding_demo
CRM Demo - AI Coding

## Backend API
- Express REST API is located in `backend/src/server.js`.
- SQLite schema lives in `docs/schema.sql` and is applied automatically on startup.
- Start the API with:
  - `cd backend`
  - `npm install`
  - `npm run start`
